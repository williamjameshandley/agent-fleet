#!/bin/sh
set -eu
[ "$#" -eq 2 ] || exit 2
slot=$1 key=$2
case "$slot" in *[!A-Za-z0-9_-]*|'') exit 2;; esac
runtime=${XDG_RUNTIME_DIR:-/run/user/$(id -u)}/agent-fleet
read -r selected _ < /proc/uptime
printf 'OPEN %s %s\n' "$key" "$selected" | /usr/bin/nc -U "$runtime/viewer-$slot.sock"
